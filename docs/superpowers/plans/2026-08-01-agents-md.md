# AGENTS.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a repository-root `AGENTS.md` with shared agent instructions and project-specific Python CLI guidance.

**Architecture:** The new Markdown document is the only deliverable. It separates reusable instructions adapted from FlixMonkey from guidance that describes this repository's Python utilities, archive behavior, dependencies, and tests. A precedence statement resolves conflicts in favor of the shared instructions except where Python best practice requires otherwise.

**Tech Stack:** Markdown, Python 3.10+, standard-library `unittest`.

## Global Constraints

- Adapt only general, non-project-specific instructions from `/home/fran/Projects/flix-monkey/AGENTS.md`.
- Shared instructions override local conventions unless doing so conflicts with Python best practice.
- Use ASCII-only prose, the Oxford comma, and no em dashes.
- Include a suggested Conventional Commit message at the end of every completed task.
- Do not modify existing application code or tests.

---

### Task 1: Create Repository Agent Guidance

**Files:**
- Create: `AGENTS.md`
- Test: `AGENTS.md` through Markdown and ASCII validation commands

**Interfaces:**
- Consumes: Project behavior in `README.md`, command-line modules, and the general rules in `/home/fran/Projects/flix-monkey/AGENTS.md`.
- Produces: Root-level guidance for future AI agents.

- [ ] **Step 1: Create `AGENTS.md` with shared and project-specific sections**

Write a document with these headings: `Purpose and Precedence`, `Project Overview`, `Setup and Verification`, `Architecture and Safety`, `Testing`, `General Working Conventions`, and `Python Conventions`. Identify the four entry-point modules, their external dependencies, archive safety requirements, dry-run behavior, and the `unittest` command. Include the inherited prose, documentation, testing, commit-message, and task-handoff rules.

- [ ] **Step 2: Validate formatting and character encoding**

Run: `git diff --check -- AGENTS.md && LC_ALL=C grep -nP '[^\\x00-\\x7F]' AGENTS.md`

Expected: `git diff --check` emits no output, and `grep` emits no output.

- [ ] **Step 3: Inspect the final document**

Run: `sed -n '1,260p' AGENTS.md`

Expected: The document contains the required shared rules, accurate project-specific instructions, and an explicit precedence statement.

- [ ] **Step 4: Commit the documentation change**

```bash
git add AGENTS.md
git commit -m "docs: add agent guidance"
```

Suggested commit message: `docs: add agent guidance`
